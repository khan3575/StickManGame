# StickManGame

Early scaffold for a JavaFX game — build setup is in place, but `Main.start()` is currently empty; no gameplay implemented yet.

## Tech stack

Java 21, JavaFX 21, Gradle

## Getting started

No Gradle wrapper is committed — run with a local Gradle install:

```bash
gradle run
```
